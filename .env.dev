DB_HOST=0.0.0.0
DB_PORT=15432
DB_USER=postgres
DB_PASSWORD=pyrpyr
DB_NAME=pyrhouse
DATABASE_URL="postgres://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?sslmode=disable"

# Database pool settings (optional - defaults shown)
# DB_MAX_OPEN_CONNS=25
# DB_MAX_IDLE_CONNS=5
# DB_CONN_MAX_LIFETIME_MINUTES=5
# DB_CONN_MAX_IDLE_TIME_MINUTES=1

# Server settings
PORT=8080
APP_HOST=localhost:${PORT}
APP_ROOT_PATH=.
APP_VERSION=1.0.0
# REQUEST_TIMEOUT_SECONDS=30

# JWT settings
JWT_SECRET=super-secret
# JWT_EXPIRATION_HOURS=120

# CORS settings (optional - comma-separated origins)
# CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5000
# CORS_MAX_AGE_HOURS=12

DISCORD_APP_ID=
DISCORD_PUBLIC_KEY=

# GOOGLE_SHEETS_CREDENTIALS_JSON= put whole json file into